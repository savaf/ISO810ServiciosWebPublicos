{
  "routes": [
    {
      "method": "GET",
      "path": "/inflation-indices/period/:period",
      "handler": "inflation-index.findOneByPeriod",
      "config": {
        "policies": []
      }
    }
  ]
}
